**Note:** Upon running the application, the ```onclickovie.py``` would render different grapphs as HTML files, that would be saved here and would be used for being displayed on the ```Plot Pulse``` page.
